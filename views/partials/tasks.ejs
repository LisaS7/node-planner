<p>Select a task:</p>
<div id="task-grid" class="grid">
  <% tasks.forEach(element => { %>
  <div class="task-cell">
    <span class="material-icons mx-2"><%= element.icon %></span>
    <%= element.name %>
  </div>
  <% }); %>
</div>
